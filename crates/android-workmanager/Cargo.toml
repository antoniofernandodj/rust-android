[package]
name    = "android-workmanager"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio   = { version = "1", features = ["sync", "time", "rt"] }
futures = "0.3"
uuid    = { version = "1", features = ["v4"] }
